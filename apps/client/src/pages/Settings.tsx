import { SettingsContainer } from '@Settings';

function Settings() {
    return (
        <div>
            Settings
            <SettingsContainer />
        </div>
    );
}

export default Settings;
