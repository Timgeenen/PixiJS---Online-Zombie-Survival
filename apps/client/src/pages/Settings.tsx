import { SettingsContainer } from '@Features/settings';

function Settings() {
    return (
        <div>
            Settings
            <SettingsContainer />
        </div>
    );
}

export default Settings;
