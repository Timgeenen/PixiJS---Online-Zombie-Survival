import { MainMenuContainer } from '@Features/mainMenu';

function MainMenu() {
    return (
        <div>
            Main Menu
            <MainMenuContainer />
        </div>
    );
}

export default MainMenu;
