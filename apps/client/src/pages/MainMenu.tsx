import { MainMenuContainer } from '@MainMenu';

function MainMenu() {
    return (
        <div>
            Main Menu
            <MainMenuContainer />
        </div>
    );
}

export default MainMenu;
