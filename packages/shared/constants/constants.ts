import type { GameDifficulty, GameModes } from '../types';

export const GAME_DIFFICULTIES: GameDifficulty[] = ['easy', 'normal', 'hard'];

export const GAME_MODES: GameModes[] = ['multiplayer', 'solo'];

export const MAX_PLAYER_LIMIT: number = 4;
